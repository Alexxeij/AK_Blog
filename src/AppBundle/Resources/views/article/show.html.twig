{% extends 'AppBundle::layout.html.twig' %}

{% block body %}

   <h3>{{ article.title }}</h3>
    <hr>
    {{ article.content }}
    <h2><a href="{{ path('article_edit',{id:article.id})}}">Edit article</a></h2>
    <h2><a href="{{ path('article_new') }}">Edit new article</a></h2>
{% endblock %}
